{
  "name": "MySQL Learning Environment",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "sleep 10 && mysql -h db -u root -ppassword < /workspace/setup.sql"
}